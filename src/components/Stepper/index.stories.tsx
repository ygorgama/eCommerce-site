import { Stepper } from ".";
import { Meta, StoryObj } from "@storybook/react";

export default {
    title: "Components/Stepper",
    component: Stepper,
} as Meta

export const Default: StoryObj = {};
import styled from "styled-components";
import { defaultTheme } from "../../styles/theme/defaultTheme";

export const ContainerBreadcumbs = styled.div`
    height: 36px;
    width: 348px;
    background-color: ${defaultTheme.colors.grey};
    border-radius: 8px;
    display: flex;
    align-items: center;
    padding: 0 .5em;
`